@page
@model WebApp.Pages_Control_PetInterests.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.PetInterest[0].Pet)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.PetInterest[0].Interest)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.PetInterest) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Pet!.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Interest!.PetInterest)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.Id">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
