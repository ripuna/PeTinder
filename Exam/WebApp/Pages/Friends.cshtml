@page
@model WebApp.Pages.Friends

@{
    ViewData["Title"] = "My Friends";
}

<div class="container mt-5">
    <h1 class="text-center">@ViewData["Title"]</h1>
    <hr />
    <div class="row justify-content-center">
        <div class="col-md-8">
            @if (Model.MyFriends == null || !Model.MyFriends.Any())
            {
                <div class="alert alert-info text-center">
                    No Friends yet :( GO and make new buddys!
                </div>
            }
            else
            {
                <ul class="list-group">
                    @foreach (var pet in Model.MyFriends)
                    {
                        <li class="list-group-item d-flex justify-content-between align-items-center">
                            <div>
                                <strong><a href="@("/Pet?id=" + pet!.Id + "&mine=" + Model.Id)" class="text-decoration-none">@pet.Name</a></strong>
                                <span class="text-muted">owned by @pet.Owner</span>
                            </div>
                            <a href="@("/Pet?id=" + pet!.Id + "&mine=" + Model.Id)" class="btn btn-primary btn-sm">View</a>
                        </li>
                    }
                </ul>
            }
        </div>
    </div>
</div>